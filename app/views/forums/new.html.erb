<%= provide(:title, 'New Forum') %>

<h1>New Forum</h1>

<%= form_for(@forum) do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<br>
	<%= f.submit "Create", class: "btn btn-large btn-primary" %>

<% end %>